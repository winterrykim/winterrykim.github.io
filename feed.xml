<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://winterrykim.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://winterrykim.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-26T02:35:33+00:00</updated><id>https://winterrykim.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Transformer and LLM Efficiency</title><link href="https://winterrykim.github.io/blog/2025/llm-eff-post/" rel="alternate" type="text/html" title="Transformer and LLM Efficiency"/><published>2025-12-23T02:00:00+00:00</published><updated>2025-12-23T02:00:00+00:00</updated><id>https://winterrykim.github.io/blog/2025/llm-eff-post</id><content type="html" xml:base="https://winterrykim.github.io/blog/2025/llm-eff-post/"><![CDATA[<p>This post summarizes the major developments for efficiency / background knowledge.</p> <h3 id="optimization-layers">Optimization Layers</h3> <ul> <li><strong>Architecture:</strong> MQA, GQA, MoE, Sparse</li> <li><strong>System / Kernel:</strong> FlashAttention (IO), PagedAttention (KV cache)</li> <li><strong>Modeling:</strong> RoPE</li> </ul> <p>Here, we go over the first layer.</p> <hr/> <h3 id="architecture">Architecture</h3> <p>Vanilla Transformer has MHA (Multi-Head Attention). The improvements are mainly made by adjusting the dimension of heads.</p> <hr/> <h4 id="mqa-multi-query-attention-2019-noam-shazeer">MQA (Multi-Query Attention) [2019, Noam Shazeer]</h4> <blockquote> <p>“However, when generating from the trained model, the output of the self-attention layer at a particular position affects the token that is generated at the next position, which in turn affects the input to that layer at the next position. This prevents parallel computation.”</p> </blockquote> <p>But we can still make incremental steps cheaper. It is mainly through removing the H dim.</p> <p>MHA (Multi-Head Attention) → MQA (Multi-Query Attention), GQA (Grouped Query Attention)</p> <div style="text-align:center; margin: 1rem 0;"> <img src="/assets/img/blog_img/llm_eff/MQA_diagram.png" alt="Multi-Query Attention (MQA) diagram" style="max-width: 900px; width: 100%; height: auto;"/> </div> <p><em>Figure: Multi-Query Attention (MQA) overview.</em></p> <hr/> <h4 id="gqa-grouped-query-attention-2023-j-ainslie-et-al">GQA (Grouped Query Attention) [2023, J Ainslie et. al]</h4> <blockquote> <p>“However, multi-query attention (MQA) can lead to quality degradation and training instability, and it may not be feasible to train separate models optimized for quality and inference.”</p> </blockquote> <div style="text-align:center; margin: 1rem 0;"> <img src="/assets/img/blog_img/llm_eff/GQA_diagram.png" alt="Group-Query Attention (GQA) diagram" style="max-width: 900px; width: 100%; height: auto;"/> </div> <ol> <li> <p>Uptrain: MHA -&gt; GQA: You can pool and combine the initial ones without having to train from scratch.</p> </li> <li> <p>GQA: Simply the middle ground between MHA and MQA. Which is why it mentions based on the count it just becomes MHA or MQA.</p> </li> </ol> <blockquote> <p>“Grouped-query attention divides query heads into G groups, each of which shares a single key head and value head. GQA-G refers to grouped-query with G groups.” “GQA-1, with a single group and therefore single key and value head, is equivalent to MQA, while GQA-H, with groups equal to number of heads, is equivalent to MHA.”</p> </blockquote> <hr/> <h4 id="mixture-of-experts-moe-why-it-shows-up-in-efficient-modeling">Mixture of Experts (MoE): why it shows up in “efficient modeling”</h4> <h5 id="main-idea--why-moe-is-efficient">Main idea / Why MoE is “efficient”</h5> <p>MoE uses <strong>conditional computation</strong>: instead of running the same dense subnetwork for every token, a <strong>router (gating network)</strong> selects a small number of <strong>experts</strong> per token. This allows “model capacity to increase without proportional increase in computational cost”.</p> <p>1) <strong>Compute efficiency (FLOPs/token):</strong><br/> You only run a small subset of experts per token (e.g., top-1 or top-2), so you avoid paying dense compute everywhere.</p> <p>2) <strong>Scaling efficiency (quality-per-compute):</strong><br/> For the <em>same</em> FLOPs budget, MoE often buys you a larger effective parameter count.</p> <h5 id="core-challenges">Core challenges</h5> <p><strong>1) Expert starvation (a.k.a. “shrinking batch”)</strong></p> <p>If you have many experts, a small batch can’t populate them well.</p> <p>Let:</p> <ul> <li>(N): total number of experts</li> <li>(K): number of selected experts per token (top-(k))</li> <li>(B): local batch size (tokens/examples) per device</li> <li>(D): number of devices</li> </ul> <p>Expected tokens per expert (very roughly):</p> <ul> <li>tokens_per_expert ≈ (K / N) * B</li> </ul> <p>If (K / N) * B « 1, most experts receive ~0 tokens → wasted capacity + inefficient execution.</p> <blockquote> <p>“This causes a naive MoE implementation to become very inefficient as the number of experts increases. The solution to this shrinking batch problem is to make the original batch size as large as possible. However, batch size tends to be limited by the memory necessary to store activations between the forwards and backwards passes.”</p> </blockquote> <p><strong>Solution: Data Parallelism + Model Parallelism</strong></p> <ul> <li> <p><strong>Data parallel</strong>: “distribute the standard layers of the model and the gating network according to conventional data-parallel schemes, but keep only one shared copy of each expert.”</p> </li> <li> <p><strong>Model parallel for experts</strong>: keep <strong>one shared copy</strong> of each expert <em>sharded across devices</em>; Each expert in the MoE layer receives a combined batch consisting of the relevant examples from all of the data-parallel input batches.</p> </li> </ul> <p>With this parallel, we go from first to second (N/K) * B « (N/K) * B * D</p> <h5 id="2-communication-bottleneck-stationary-experts">2) Communication bottleneck (stationary experts)</h5> <p>Experts are typically <strong>stationary</strong> on certain devices. Tokens must be <strong>dispatched</strong> to the expert’s device and then <strong>collected</strong> back.<br/> If each expert does only a tiny amount of compute, GPUs can become <strong>network-bound</strong> (waiting on transfers)</p> <p><strong>Solution: Increase compute-per-byte (arithmetic intensity)</strong><br/> “To maintain computational efficiency, the ratio of an expert’s computation to the size of its input and output must exceed the ratio of computational to network capacity of the computing device.’</p> <p>The point is not “communication time decreases.”<br/> It’s: <em>if communication is fixed, make the expert compute large enough that communication becomes hidden/overlapped and no longer dominates step time.</em></p> <p>A useful mental ratio is:</p> <p><code class="language-plaintext highlighter-rouge">(expert_compute / activation_bytes_moved) &gt; (device_compute / network_bandwidth)</code></p> <p>“Conveniently, we can increase computational efficiency simply by using a larger hidden layer, or more hidden layers.”</p> <h4 id="3-load-balancing-avoid-routing-collapse">3) Load balancing (avoid routing collapse)</h4> <p>Without constraints, the router might send too many tokens to a few experts.</p> <p>Common strategies:</p> <ul> <li><strong>Auxiliary balancing losses</strong> (e.g., importance / load losses based on coefficient of variation)</li> <li><strong>Noise on routing logits</strong> (e.g., Gaussian noise) to encourage exploration early</li> <li><strong>Capacity limits</strong> per expert (drop/overflow strategies)</li> </ul> <p>Goal: keep usage reasonably spread so all experts train and hardware stays utilized.</p> <hr/> <h3 id="switch-transformer-why-moe-pairs-naturally-with-transformers">(Switch Transformer) Why MoE pairs naturally with Transformers</h3> <p>RNN-style model has strong sequential dependencies across timesteps during training that makes “one giant batch over time” harder to exploit. The limitation of the MoE as athor suggested “we wait for the previous layer to finish, we can apply the MoE to all the time steps together as one big batch” If you try to put an MoE inside an RNN (like an LSTM), the “one timestep depends on the output of the MoE at the previous timestep”</p> <p>In contrast, Transformers enable large, parallel token computation during training (within a sequence and across sequences), which makes it easier to form <strong>big token batches</strong> for MoE routing.</p> <blockquote> <p>“we investigate a fourth axis: increase the parameter count while keeping the floating point operations (FLOPs) per example constant” - Switch Transformers [2021]</p> </blockquote> <p>This is one practical reason MoE is often discussed as “Transformer + sparse FFN”:</p> <ul> <li>keep attention dense (or not—orthogonal choice),</li> <li>swap dense FFN blocks for <strong>MoE / Switch FFN</strong> blocks.</li> </ul> <hr/> <h3 id="thoughts">Thoughts</h3> <p>I believe these philosophies are very useful for scientific models, especially as we scale them up. For example, Model parameters for genomic language models are also slowly increasing, and the same ideas should be useful for catching up with this trend.</p> <ul> <li>This naturally raises the question I’m trying to explore next: how alternative generation regimes (e.g., <strong>Diffusion Language Models</strong>) shift the bottlenecks—memory, communication, and parallelism.</li> </ul> <p><strong>Key references</strong></p> <ul> <li><strong>[2019] <em>Fast Transformer Decoding: One Write-Head is All You Need</em></strong> — Noam Shazeer</li> <li> <p><strong>[2023] <em>GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints</em></strong> — Joshua Ainslie, James Lee-Thorp, Michiel de Jong, Yury Zemlyanskiy, Federico Lebrón, Sumit Sanghai</p> </li> <li><strong>[2017] <em>Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer</em></strong> — Shazeer et al.</li> <li><strong>[2021] <em>Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity</em></strong> — Fedus et al.</li> <li><strong>[2022] <em>Sparse Upcycling: Training Mixture-of-Experts from Dense Checkpoints</em></strong> — Komatsuzaki et al.</li> </ul>]]></content><author><name></name></author><category term="technical-blogs"/><category term="ml"/><category term="notes"/><summary type="html"><![CDATA[Summary Notes on Catching Up]]></summary></entry></feed>